id: TC-005
type: technical-constraint
title: "Post-merge suspect link detection"

description: >
  The system must automatically mark downstream requirement cards as suspect
  after a merge that changes upstream requirements.

rationale: "Pending owner review"

fit_criteria:
  user:
    criterion: >
      Post-merge hook finds changed requirement cards, marks dependents as
      suspect via suspect.sh, flags cross_verify targets, runs validation.
      Advisory only — always exits 0.
    verification: test
    test_type: integration
    verification_method: integration

dal: C
priority: should
status: proposed

origin:
  stakeholder: extract-requirements
  date: "2026-03-31"
  trigger: "Codebase extraction"
  source: "plugin/hooks/check-merge.sh"

serves:
  - UR-008
