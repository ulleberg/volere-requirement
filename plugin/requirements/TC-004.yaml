id: TC-004
type: technical-constraint
title: "Branch requirement drift detection"

description: >
  The system must warn when requirement cards or DAL configuration differ
  between the branch being checked out and the current branch.

rationale: "Pending owner review"

fit_criteria:
  user:
    criterion: >
      Post-checkout hook detects DAL mismatch and changed requirement cards
      between branches. Advisory only — always exits 0. Skips file checkouts
      and initial checkouts.
    verification: test
    test_type: integration
    verification_method: integration

dal: C
priority: should
status: proposed

origin:
  stakeholder: extract-requirements
  date: "2026-03-31"
  trigger: "Codebase extraction"
  source: "plugin/hooks/check-checkout.sh"

serves:
  - UR-005
